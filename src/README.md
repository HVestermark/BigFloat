# Source

This directory will contain the project's source code (e.g., Rust/C/Go/JS).

It's added as a placeholder so the directory exists in the repository. Replace this README or add source files under `src/` as you start implementing the library.
